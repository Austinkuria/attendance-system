# System Architecture - Visual Overview

**Date**: November 16, 2025

---

## 🏗️ Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         ATTENDANCE SYSTEM                            │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                          USER HIERARCHY                              │
└─────────────────────────────────────────────────────────────────────┘

                        ┌──────────────────┐
                        │   SUPER ADMIN    │
                        │  (System-wide)   │
                        └────────┬─────────┘
                                 │
                  ┌──────────────┼──────────────┐
                  │              │              │
         ┌────────▼────────┐ ┌──▼────────┐ ┌──▼────────┐
         │  Dept Admin     │ │ Dept Admin│ │ Dept Admin│
         │   (Business)    │ │  (Health) │ │(Engineering)│
         └────────┬────────┘ └──┬────────┘ └──┬────────┘
                  │              │              │
      ┌───────────┼──────────┐   │              │
      │           │          │   │              │
  ┌───▼───┐  ┌───▼───┐  ┌───▼───▼──┐      ┌───▼───┐
  │Student│  │Student│  │ Lecturer │      │Lecturer│
  │(BUS)  │  │(BUS)  │  │  (BUS)   │      │ (ENG) │
  └───────┘  └───────┘  └──────────┘      └────────┘

```

---

## 🔐 Access Control Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                       REQUEST FLOW                                   │
└─────────────────────────────────────────────────────────────────────┘

User Request: "Create Student in Business Dept"
     │
     ▼
┌─────────────────────┐
│  Authentication     │ ← JWT Token Validation
│  Middleware         │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Authorization      │ ← Check Role (admin, dept_admin, etc.)
│  Middleware         │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Department Check   │ ← Is Business in managedDepartments?
│  Middleware         │
└──────────┬──────────┘
           │
           ├─── YES ──► Continue to Controller
           │
           └─── NO ───► 403 Forbidden
                        "Access denied: You don't have permission..."
```

---

## 📊 Student Creation Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│              STUDENT CREATION WITH AUTO-ENROLLMENT                   │
└─────────────────────────────────────────────────────────────────────┘

Dept Admin: "Create Student"
  │
  │ POST /api/students
  │ {
  │   firstName: "Jane",
  │   department: "Business",
  │   course: "Business Admin",
  │   year: 2,
  │   semester: 1
  │ }
  │
  ▼
┌─────────────────────────────────┐
│ 1. Validate Department Access   │ ✅ Admin manages Business? YES
└─────────────┬───────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│ 2. Create User Record           │ → Save to MongoDB
│    - Hash password              │
│    - Set isVerified = true      │
│    - Set mustChangePassword = true
│    - Set createdBy = adminId    │
└─────────────┬───────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│ 3. Auto-Enroll in Units         │
│    Query:                       │
│    - course = "Business Admin"  │
│    - year = 2                   │
│    - semester = 1               │
│    - isActive = true            │
└─────────────┬───────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│ Found 5 Units:                  │
│ • BUS201 - Business Analytics   │
│ • BUS202 - Marketing            │
│ • BUS203 - Finance              │
│ • BUS204 - Management           │
│ • BUS205 - Economics            │
└─────────────┬───────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│ 4. Update Records               │
│    Student.enrolledUnits = [...]│
│    Unit.studentsEnrolled += [...│
└─────────────┬───────────────────┘
              │
              ▼
┌─────────────────────────────────┐
│ 5. Response to Admin            │
│ {                               │
│   success: true,                │
│   user: { id, name, ... },      │
│   enrollment: {                 │
│     enrolledUnits: 5,           │
│     units: [...]                │
│   }                             │
│ }                               │
└─────────────────────────────────┘
              │
              ▼
        ✅ COMPLETE!
   Student ready to attend classes
```

---

## 🛡️ Department Isolation

```
┌─────────────────────────────────────────────────────────────────────┐
│                  DEPARTMENT ISOLATION EXAMPLE                        │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                    DATABASE STATE                                 │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  Dept Admin John:                                                │
│    managedDepartments: [Business_ID, Marketing_ID]               │
│                                                                   │
│  Students:                                                        │
│    • Alice (department: Business_ID)      ✅ John can access     │
│    • Bob   (department: Marketing_ID)     ✅ John can access     │
│    • Carol (department: Health_ID)        ❌ John CANNOT access  │
│    • Dave  (department: Engineering_ID)   ❌ John CANNOT access  │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘

John's Request: GET /api/students
                ↓
┌──────────────────────────────────────────────────────────────────┐
│ Department Middleware automatically filters:                     │
│                                                                   │
│ const accessibleDepts = await getAccessibleDepartments(john);   │
│ // Returns: [Business_ID, Marketing_ID]                         │
│                                                                   │
│ const students = await User.find({                              │
│   role: 'student',                                              │
│   department: { $in: [Business_ID, Marketing_ID] }             │
│ });                                                              │
│                                                                   │
│ Result: Returns only Alice and Bob                              │
│         Carol and Dave are HIDDEN from John                     │
└──────────────────────────────────────────────────────────────────┘
```

---

## 📁 File Structure

```
backend/
│
├── middleware/
│   ├── authMiddleware.js             ← JWT authentication
│   ├── authorizeMiddleware.js        ← Role-based access
│   └── departmentAuthMiddleware.js   ← 🆕 Department isolation
│
├── controllers/
│   ├── userController.js             ← Enhanced with auto-enrollment
│   ├── authController.js             ← Login/logout
│   └── superAdminController.js       ← 🆕 Dept admin management
│
├── routes/
│   ├── userRoutes.js                 ← ⚠️ Signup disabled
│   ├── studentRoutes.js              ← Student CRUD
│   ├── auth.routes.js                ← Authentication
│   ├── superAdminRoutes.js           ← 🆕 Super admin endpoints
│   └── index.js                      ← Main router
│
├── models/
│   └── User.js                       ← Enhanced with managedDepartments
│
├── utils/
│   ├── authUtils.js                  ← JWT utilities
│   └── enrollment.utils.js           ← 🆕 Auto-enrollment system
│
├── scripts/
│   ├── seedSuperAdmin.js             ← Create first super admin
│   └── updateToSuperAdmin.js         ← Promote user to super admin
│
└── services/
    └── email.service.js              ← Email sending (verification, etc.)
```

---

## 🔄 Data Flow Diagram

```
┌────────────────────────────────────────────────────────────────────┐
│                    COMPLETE DATA FLOW                               │
└────────────────────────────────────────────────────────────────────┘

1. SUPER ADMIN CREATES DEPT ADMIN
   ┌──────────┐    POST /super-admin/department-admins    ┌──────────┐
   │  Super   │ ───────────────────────────────────────►  │ Backend  │
   │  Admin   │                                            │  API     │
   └──────────┘                                            └────┬─────┘
                                                                │
                                                                ▼
                                                         ┌──────────────┐
                                                         │  MongoDB     │
                                                         │  User:       │
                                                         │  - role:     │
                                                         │    dept_admin│
                                                         │  - managed:  │
                                                         │    [Bus, Mkt]│
                                                         └──────────────┘

2. DEPT ADMIN CREATES STUDENT
   ┌──────────┐      POST /students                      ┌──────────┐
   │   Dept   │ ───────────────────────────────────────►  │ Backend  │
   │  Admin   │                                            │  API     │
   └──────────┘                                            └────┬─────┘
                                                                │
                                        ┌───────────────────────┴──────┐
                                        ▼                              ▼
                                 ┌──────────────┐            ┌──────────────┐
                                 │  Save Student│            │ Auto-Enroll  │
                                 │  to MongoDB  │            │ in Units     │
                                 └──────────────┘            └──────┬───────┘
                                                                    │
                                                                    ▼
                                                            ┌──────────────┐
                                                            │ Update Units │
                                                            │ - Add student│
                                                            │   to rosters │
                                                            └──────────────┘

3. STUDENT ATTENDS CLASS
   ┌──────────┐      Scan QR Code                         ┌──────────┐
   │ Student  │ ───────────────────────────────────────►   │ Lecturer │
   │          │                                             │  Device  │
   └──────────┘                                             └────┬─────┘
                                                                 │
                                                                 ▼
                                                          ┌──────────────┐
                                                          │ Verify       │
                                                          │ Enrollment   │
                                                          └──────┬───────┘
                                                                 │
                                                  ┌──────────────┴─────────┐
                                                  │  ✅ Enrolled?          │
                                                  │  ✅ In right class?   │
                                                  │  ✅ Right time?       │
                                                  └────────────────────────┘
                                                                 │
                                                                 ▼
                                                          ┌──────────────┐
                                                          │  Mark as     │
                                                          │  PRESENT     │
                                                          └──────────────┘
```

---

## 🔍 Security Layers

```
┌────────────────────────────────────────────────────────────────────┐
│                       SECURITY LAYERS                               │
└────────────────────────────────────────────────────────────────────┘

Layer 1: AUTHENTICATION
  ├─► JWT Token Validation
  ├─► Token Expiry Check (15min access, 7d refresh)
  ├─► User Active Status
  └─► Email Verified Status

Layer 2: AUTHORIZATION
  ├─► Role Check (student, lecturer, dept_admin, super_admin)
  ├─► Route Permissions
  └─► Action Permissions (create, read, update, delete)

Layer 3: DEPARTMENT ISOLATION
  ├─► Super Admin: Access ALL departments ✅
  ├─► Dept Admin: Access ONLY managed departments ✅
  ├─► Lecturer: Access ONLY own department ✅
  └─► Student: Access ONLY own department ✅

Layer 4: DATA VALIDATION
  ├─► Input Sanitization
  ├─► Type Checking
  ├─► Required Fields
  └─► Business Logic Validation

Layer 5: AUDIT TRAIL
  ├─► createdBy field tracking
  ├─► lastLogin timestamps
  ├─► Action logging
  └─► Security events logging
```

---

## 📈 Scalability

```
Current System Can Handle:
├─► Unlimited Departments
├─► Unlimited Department Admins (per department)
├─► Unlimited Students (per department)
├─► Unlimited Lecturers (per department)
├─► Unlimited Units (per course)
└─► Department Admins can manage multiple departments

Performance Optimizations:
├─► Indexed Fields:
│   ├─► email + isActive
│   ├─► role + department
│   ├─► managedDepartments
│   └─► course + year + semester
│
├─► Efficient Queries:
│   ├─► Department filtering at DB level
│   ├─► Pagination support (future)
│   └─► Aggregation pipelines (for stats)
│
└─► Caching Ready:
    ├─► User permissions
    ├─► Department lists
    └─► Unit enrollments
```

---

## 🎯 Summary

This architecture provides:

✅ **Complete Hierarchy**: Super Admin → Dept Admin → Users  
✅ **Department Isolation**: Cross-dept access prevented  
✅ **Auto-Enrollment**: Students immediately ready  
✅ **Scalable**: Handles unlimited departments  
✅ **Secure**: Multiple security layers  
✅ **Auditable**: Full action tracking  

**Status**: ✅ Production Ready

For implementation details, see `ADMIN_HIERARCHY_GUIDE.md`
